# 贡献指南

感谢你考虑为 AI 自动化代码流水线项目做出贡献!

## 🎯 贡献方式

### 报告 Bug

如果你发现了 bug,请创建一个 Issue 并包含:

- 清晰的标题和描述
- 重现步骤
- 期望行为 vs 实际行为
- 环境信息 (Python 版本、操作系统等)
- 相关日志或截图

### 提出新功能

在创建 Feature Request 之前:

1. 搜索现有 Issues,避免重复
2. 清楚描述功能的用途和价值
3. 如果可能,提供实现思路

### 提交代码

1. **Fork 仓库**

   ```bash
   git clone https://github.com/your-username/ai-pipeline.git
   cd ai-pipeline
   ```

2. **创建分支**

   ```bash
   git checkout -b feature/your-feature-name
   ```

   分支命名规范:
   - `feature/` - 新功能
   - `bugfix/` - Bug 修复
   - `docs/` - 文档更新
   - `refactor/` - 代码重构

3. **编写代码**

   请遵循以下规范:

   - **代码风格**: 遵循 PEP 8
   - **命名**: 使用清晰的变量和函数名
   - **注释**: 为复杂逻辑添加注释
   - **文档字符串**: 为公共 API 添加 docstring

4. **添加测试**

   ```bash
   # 为你的功能添加单元测试
   # 测试文件放在 tests/ 目录下

   pytest tests/ -v
   ```

5. **运行代码检查**

   ```bash
   # 代码格式化
   pip install black
   black orchestrator/ example_app/

   # Linter 检查
   pip install flake8
   flake8 orchestrator/ example_app/
   ```

6. **提交更改**

   提交信息规范:

   ```
   <type>(<scope>): <subject>

   <body>

   <footer>
   ```

   类型 (type):
   - `feat`: 新功能
   - `fix`: Bug 修复
   - `docs`: 文档更新
   - `style`: 代码格式调整
   - `refactor`: 代码重构
   - `test`: 测试相关
   - `chore`: 构建/工具链更新

   示例:

   ```bash
   git commit -m "feat(orchestrator): 添加重试机制

   - 为 LLM API 调用添加指数退避重试
   - 最大重试次数可配置
   - 添加相关单元测试

   Closes #123"
   ```

7. **推送到远程**

   ```bash
   git push origin feature/your-feature-name
   ```

8. **创建 Pull Request**

   - 填写 PR 模板
   - 关联相关 Issues
   - 等待代码审查

## 🧪 测试指南

### 运行测试

```bash
# 运行所有测试
pytest

# 运行特定测试
pytest tests/test_specific.py

# 生成覆盖率报告
pytest --cov=orchestrator --cov=example_app --cov-report=html
```

### 编写测试

- 每个新功能都应有对应测试
- 测试应该独立且可重复
- 使用清晰的测试命名: `test_<功能>_<场景>`

示例:

```python
def test_add_endpoint_with_valid_input_returns_correct_sum():
    """测试加法端点在有效输入下返回正确结果"""
    client = app.test_client()
    response = client.post('/add', json={'a': 1, 'b': 2})
    assert response.status_code == 200
    assert response.json['result'] == 3
```

## 📝 文档更新

如果你的更改影响了:

- API 接口
- 配置选项
- 使用方式

请同步更新:

- `README.md`
- 相关代码的 docstring
- 示例代码

## 🔍 代码审查流程

提交 PR 后:

1. 自动 CI 检查会运行
2. 维护者会审查你的代码
3. 根据反馈进行修改
4. 审查通过后合并

审查标准:

- ✅ 代码质量和可读性
- ✅ 测试覆盖率
- ✅ 文档完整性
- ✅ 是否遵循项目规范

## 🎨 代码风格

### Python

```python
# 好的示例
def calculate_total_price(items: List[Item], discount: float = 0.0) -> float:
    """
    计算总价格

    Args:
        items: 商品列表
        discount: 折扣率 (0.0 - 1.0)

    Returns:
        总价格
    """
    subtotal = sum(item.price for item in items)
    return subtotal * (1 - discount)


# 不好的示例
def calc(i, d=0):
    return sum([x.p for x in i]) * (1-d)
```

### 提示词模板

- 使用清晰的结构和标记
- 提供充分的上下文
- 包含示例和约束

## 🚀 发布流程

(仅适用于维护者)

1. 更新版本号
2. 更新 CHANGELOG.md
3. 创建 Git tag
4. 发布到 PyPI (如适用)

## ❓ 需要帮助?

- 📖 查看 [README.md](README.md)
- 💬 在 [GitHub Discussions](https://github.com/your-repo/discussions) 提问
- 📧 联系维护者

## 📜 行为准则

- 尊重所有贡献者
- 建设性反馈
- 专注于技术讨论
- 接受不同观点

---

再次感谢你的贡献! 🎉
